version: '3.8'
services:
  bxdp-backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5001:5001"
    environment:
      - NODE_ENV=development
      - SAFE_MODE=true
    volumes:
      - .:/code
      - ./.mock_uploads:/code/.mock_uploads
